specVersion: "v1beta"
serviceId: "cookie"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cookie-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
